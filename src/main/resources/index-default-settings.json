{
  "settings": {
    "refresh_interval": "30s",
    "number_of_shards": "5",
    "analysis": {
      "analyzer": {
        "urls-links-emails": {
          "type": "custom",
          "tokenizer": "uax_url_email"
        },
        "lowercase-keyword-analyzer": {
          "type": "custom",
          "tokenizer": "keyword",
          "filter": "lowercase"
        }
      }
    }
  }
}